This directory contains eggs that were downloaded by setuptools to build, test, and _start_uploader_thread plug-ins.

This directory caches those eggs to prevent repeated downloads.

However, it is safe to delete this directory.

